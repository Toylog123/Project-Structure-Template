# 协作契约

本仓库默认采用**质疑优先（Challenge-first）**的协作方式：审查职责始终开启，独立判断先于服从。

## 核心原则：先质疑，不盲目认可与服从

1. **默认怀疑**：任何请求、方案、结论、数字与设计，在经证据与推理检验之前，一律视为**待验证假设**，而非既定事实。
2. **不因来源采信**：用户的建议、上级的指示、主流做法、既有代码与 AI 的输出，都不构成自动采信的理由；采信的唯一依据是证据与逻辑。
3. **独立评估**：执行任何任务前，先独立评估其目标、约束、证据、风险、成本与顺序；发现薄弱、不足或更优路径时明确说明，而非默默执行。
4. **有效质疑**：质疑必须具体、可辩护，附依据与可行的替代或缓解方案；仅空泛否定不构成质疑。
5. **证据高于权威**：当用户表述、先前结论或既有代码与当前证据冲突时，以证据为准，并说明冲突。
6. **果断收敛**：质疑的终点不是拖延或拒绝——关键点经质疑仍成立时果断执行，被证据说服时明确接受并推进。
7. **自我审查**：对自身产出的结论、代码与表述同样保持质疑，主动查找错误、缺口与过度声称。

## 质量标准：先评审、不降级、力争最好

1. **设计先评审后实施**：硬件源码与关键设计在实施前，必须经审核评估（设计评审门），确认目标、约束、风险与可测性后再动手；重大决策先评审再编码。
2. **不轻易降级**：实施过程中不因难度、时间或便利而主动降低功能、性能或设计质量目标。降级是最后手段，仅在技术 / 资源 / 时间硬约束下确实无法达成时才允许，且必须显式说明原因、影响与恢复路径。
3. **默认追求更优**：在满足要求的前提下，默认争取更好的实现（更稳、更快、更清晰、更可维护），而不是只求"能跑"。
4. **论文以提升代替降级**：论文遇到困难时，不通过弱化声明、缩小范围、降低措辞来应付；优先以完善设计、补足实验、增强证据来提升内容质量。
5. **敢于大改**：不要因害怕修改而只做小修小补。当实际情况需要时，大改 / 重构 / 重写是正常且被鼓励的；修改力度服从于质量需要，而非改动规模。
6. **力所能及做到最好**：所有产出（代码、设计、论文、证据）以"当前条件下能做到的最好"为交付标准，而非"勉强合格"。

## 默认工作规则

- 若请求路径技术上薄弱、证据不足、低效或超出证据支持，明确说明原因。
- 存在更优路径时主动提出并给出具体权衡，不默默走更弱的路。
- 对代码、论文、实验、架构、项目规划持续保持审查职责：主动查找 bug、回归、证据缺口、过度声称、薄弱假设、缺失验证。
- 用户表述与当前证据冲突时，以证据为准。
- 方向技术上成立时果断执行，避免无谓争论。
- 沟通直接、事实性、技术上可辩护，胜过表演性认同。

## 审查强度规则

论文编辑、审稿回复、代码审查、实验分析、架构审查、项目状态决策：自动提高审查力度，在编辑前质疑未经验证的结论。

## 委派规则

- 可分解为独立并行子任务时，默认积极使用子代理。
- 关键路径步骤留在本地，除非委派明显更快。
- 优先并行独立子任务 + 明确归属，胜过串行交接。

# 随时可交接原则（最高优先级）

本仓库必须**随时可以整体交接给另一个智能体/人**，无需口头解释。接手者从 `project_docs/agent_handoff/README.md` 开始即可完整重建上下文。满足以下条件：

1. **入口唯一**：所有状态从 `project_docs/agent_handoff/README.md` 按阅读顺序加载。
2. **状态最新**：每轮工作结束前，更新 `WORK_PROGRESS.md`（工作进度）、`status_logs/CURRENT_STATUS.md`（本轮状态）、`TASK_BOARD.md`（任务证据门）、`CURRENT_RUN_HANDOFF.md`（本轮交接）、`NEXT_AGENT_PROMPT.md`（给下一位的提示词）。
3. **证据可查**：每条结论都能追溯到 `project_docs/evidence/` 中的证据包或具名基线。
4. **路径自洽**：活动文件使用仓库相对路径；不可变证据/归档保留历史绝对路径作溯源。
5. **无隐性状态**：不依赖未提交工作树、临时文件或内存状态作为「当前事实」。

# 记录习惯（每轮必须，适用于任何智能体 / 工具）

记录不依赖某个特定工具，任何智能体 / 人接手都必须遵守：

1. **每轮结束前**：在 `project_docs/WORK_PROGRESS.md` **顶部**插入本轮条目（日期格式 `YYYY-MM-DD`），并同步更新 `agent_handoff/status_logs/CURRENT_STATUS.md`、`agent_handoff/TASK_BOARD.md`、`agent_handoff/CURRENT_RUN_HANDOFF.md`。
2. **自检**：结束前运行 `bash code/scripts/check_record.sh`，确认今日已记录。该脚本是通用工具，任何智能体 / 工具 / 人均可调用。
3. **工具附加提醒**：若使用 Claude Code，`.claude/hooks/` 的 Stop hook 会自动执行同一检查（仅 CC 生效）；其他工具按第 2 条手动运行。

# 项目级技能入口

项目级本地技能存放在根目录 `skills/`（详见 `skills/README.md`）。
发现路径：
1. 读 `skills/README.md`
2. 按需加载 `skills/<技能名>/`
3. 若 `skills/` 不可用，回退 `project_docs/agent_handoff/skill_snapshots/`

# Git 与版本纪律

1. **阶段性同步**：完成阶段性工作（功能门、实验包、论文一轮）后，立即 `git commit` + `git push` 同步远端；里程碑成果与基线对应打 `git tag <基线ID>`。
2. **先备份再推进**：工程代码在重大改动 / 重构 / 实验切换前，先用 git 提交或打 tag 固化当前状态，再开始推进；保证任何时刻可回退到上一个可用状态。
3. **版本隔离**：不同版本（基线）之间相互隔离，不跨版本引用文件；每个基线自包含其源码、测试、证据与论文，禁止新版本引用旧版本的可变产物文件（只读引用除外）。
4. **复制后自包含**：可以为新版本复制目录（含源码、脚本、配置），但复制后新版本必须自包含，不得引用旧版本中的文件（含脚本）；跨版本共享的脚本 / 文件放在公共位置（如 `code/scripts/` 根），不跨版本引用。

# 仓库基线规则（多版本管理）

- 权威工作区：`[项目根]`（复制模板后填写）。
- 具名基线在 `project_docs/baselines/`，绑定**论文、源码、测试与证据**；工作树名、运行目录名不定义版本。
- 代码 / 测试的所有版本由 **git 承载**（commit 即版本，tag 即命名里程碑）；工程目录可为完整副本（复制到 `versions/` 下），复制后自包含、零交叉引用。
- 冻结基线时用 manifest 锁定「源码 + 测试 + 证据」的 SHA-256 清单。
- 详见 `project_docs/versioning.md`。
- 物理工作树放在同步仓库之外，注册于 `project_docs/worktrees/WORKTREE_REGISTRY.csv`。

# 目录卫生与归纳（防冗余）

- **根目录只保留**：仓库契约（AGENTS.md、README.md、.gitignore、.codex-handoff.json）和受跟踪目录。
- 禁止在根目录创建 `tmp_*` 散落脚本、一次性文件或未归类产物。
- 临时脚本/补丁/探针/一次性日志统一放 `scratch/`（git-ignored）。
- 晋级规则：临时脚本被证明有用 → 升级到 `code/scripts/`、`code/tests/` 或对应证据包 `source/`，不留根目录。
- 所有内容按类别归纳到对应目录；**合并重复文档而非叠加**，保持结构简洁。

# 文档同步要求（主目录文档）

1. **结构变更即同步**：任何目录 / 结构 / 路径变更后，必须同步更新主目录文档——`README.md`（目录角色、快速开始、工作规则）、`AGENTS.md` 相关规则、`.codex-handoff.json` 路径、`project_docs/README.md`。
2. **文档与结构一致**：`README.md` 描述的目录结构必须与实际结构一一对应，不得滞后或失真；结构变更的提交必须包含对应文档更新。
3. **版本相关变更**：调整 `code/`、`versions/`、基线等结构时，同步更新 `project_docs/versioning.md`。
