# English version (en)

> 英文版论文目录。**仅在投稿英文期刊 / 会议时维护**。

- `manuscript/`：英文主稿件（由 `../zh/manuscript/` 转换）
- `responses/`：英文审稿回复
- `figures/`、`scripts/`：与 `zh/` 对齐

转换规则：以 `zh/` 为主版本，两版结论必须一致；改数字时同步两版与 `project_docs/evidence/CROSSWALK_TEMPLATE.md`。
