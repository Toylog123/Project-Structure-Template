# Worktree Policy

工作树是一次性执行环境，**不是事实来源**。

- 物理根：`[项目根之外，如 D:\<PROJECT>_worktrees\]`
- 注册表：`WORKTREE_REGISTRY.csv`
- 每个工作树记录：命名基线、用途、创建时间、预期证据去向、退役状态。
- 结果只有在复制为 `project_docs/evidence/` 下的自包含包并登记索引后才算持久。
- 论文引用具名基线 / 证据包，绝不引用工作树路径。
- 不要在同步仓库内创建工作树目录。
