# 数据治理

本目录用于数据集治理，而非结论存档。

- `manifests/`：来源 URL、获取日期、许可、大小、SHA-256。
- `raw/`：不可变源捕获，Git 默认忽略。
- `derived/`：可复现的中间数据集，Git 默认忽略。
- `schemas/`：受跟踪的字段与文件格式定义。

仅当有真实内容时才创建子目录。证据包应把复现所需的最小精确输入复制进自身 `input/` 目录，而不是依赖 `data/raw/` 的全局数据。
