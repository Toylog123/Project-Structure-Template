# project — 构建工程与运行配置

本目录存放项目工程的构建/运行入口，而非源码。

- `scripts/`：启动、构建、状态检查脚本（受 git 跟踪）。
- 生成的工程文件（工程目录、日志、中间产物）**一律 git-ignored**（`.gitignore` 已配置 `project/*/`），只保留本说明与 `scripts/`。

规则：

1. 生成工程用脚本一键创建，删除后可用脚本重建，不要手工维护生成物。
2. 运行日志、状态输出放入 `scratch/` 或对应证据包，不要散落在 `project/` 根下。
