# 示例实验：example_case

> 多级目录示例：`experiments/<版本>/<类型>/<实验>/`。
> 复制为实际实验，替换字段。

## 版本 / 类型 / 实验

- 一级版本：v1
- 二级类型：rtl
- 三级实验：example_case

## 实验说明（每项必答）

- 基线版本（绑定哪份代码 / 基线）：
- 输入清单（数据来源、manifest）：
- 命令（如何运行，见 `scripts/`）：
- 期望输出：
- 证据去向（`project_docs/evidence/v1/rtl/example_case/<日期>_r<N>/`）：
- **反驳性测试计划（自杀式测试）**：什么结果会推翻本主张？最强竞争基线、失效场景、不利初始条件？

## 目录

- `config/`：实验配置
- `scripts/`：实验脚本
