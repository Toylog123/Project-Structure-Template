# 示例模块：module_a

> 展示一个源码模块的形式。复制为 `src/<模块名>/`，保留 `.gitkeep` 之外的说明可删除。

| 文件 | 说明 |
|------|------|
| `<模块名>.c/.py/.sv/...` | 主源码（按技术栈） |
| `include/` 或 `*.h` | 公共接口 / 定义 |
| `README.md` | 模块职责与接口（可选） |
| `.gitkeep` | 空目录占位 |

模块 README 建议写：模块职责、关键接口、依赖、对应设计表（`src/DESIGN.md`）。
